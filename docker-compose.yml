services:
  purple-agent:
    build: .
    ports:
      - "9009:9009"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_API_KEY_IMAGE_GEN=${GEMINI_API_KEY_IMAGE_GEN}
    command: [ "npx", "tsx", "server.ts" ]
